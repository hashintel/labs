{
  "name": "@hashintel/utils",
  "version": "0.0.1",
  "private": true,
  "description": "A collection of utils for use in HASH projects",
  "scripts": {
    "prepublishOnly": "tsc -b .",
    "postinstall": "tsc -b .",
    "build": "tsc -b .",
    "build:watch": "tsc -b . -w",
    "fmt": "prettier --write \"*.{ts,js,json}\" \"{scripts,src}/**/*.{ts,js,json}\" && eslint --quiet --fix \"*.{ts,js,json}\" \"{scripts,src}/**/*.{ts,js,json}\"",
    "fmt-check": "prettier --check \"*.{ts,js,json}\" \"{scripts,src}/**/*.{ts,js,json}\" && eslint --quiet \"*.{ts,js,json}\" \"{scripts,src}/**/*.{ts,js,json}\""
  },
  "license": "SSPL-1.0",
  "devDependencies": {
    "@types/node": "^14.14.7",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@types/papaparse": "5.2.5",
    "papaparse": "5.3.0"
  }
}
