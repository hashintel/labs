import React from "react";
import { render } from "@testing-library/react";

import { IconContentDuplicate } from "./IconContentDuplicate";

it("renders without crashing", () => {
  render(<IconContentDuplicate />);
});
