import React from "react";
import { render } from "@testing-library/react";

import { IconCheckboxMarkedOutline } from "./IconCheckboxMarkedOutline";

it("renders without crashing", () => {
  render(<IconCheckboxMarkedOutline />);
});
