import React from "react";
import { render } from "@testing-library/react";

import { IconDragVertical } from "./IconDragVertical";

it("renders without crashing", () => {
  render(<IconDragVertical />);
});
