import React from "react";
import { render } from "@testing-library/react";

import { IconPresentationPause } from "./IconPresentationPause";

it("renders without crashing", () => {
  render(<IconPresentationPause />);
});
