import React from "react";
import { render } from "@testing-library/react";

import { IconArrowDownDrop } from "./IconArrowDownDrop";

it("renders without crashing", () => {
  render(<IconArrowDownDrop />);
});
