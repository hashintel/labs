const CLAUDE_GITIGNORE = `# Generated by agentprofiles
# Keep: settings.json, CLAUDE.md, agents/, skills/, commands/, plans/, double-shot-latte/
# Ignore: caches, sessions, local state, auth tokens, machine-specific files

# Caches and temporary data
cache/
debug/
downloads/
file-history/
ide/
paste-cache/
shell-snapshots/
stats-cache.json
statsig/

# Session and history
history.jsonl
session-env/

# Local state and projects
projects/
todos/

# Machine-specific and sensitive files
.DS_Store
.claude.json
.claude.json.backup
settings.local.json
`;

const OPENCODE_GITIGNORE = `# Generated by agentprofiles
# Keep: opencode.json, opencode.jsonc, agents/, plugins/, commands/
# Ignore: node_modules, lock files, auto-generated .gitignore

# Dependencies and lock files
node_modules/
bun.lock
package.json

# Auto-generated files
.gitignore

# System files
.DS_Store
`;

const AMP_GITIGNORE = `# Generated by agentprofiles
# Keep: settings.json, AGENTS.md, themes/
# Ignore: oauth tokens, caches, logs, temporary files

# Authentication and sensitive data
oauth/

# Caches and logs
cache/
logs/

# Temporary files
tmp/

# System files
.DS_Store
`;

const CODEX_GITIGNORE = `# Generated by agentprofiles
# Keep: config.toml, rules/, prompts/, skills/ (contains symlinks to ~/.agents/skills/)
# Ignore: auth tokens, caches, sessions, local state, machine-specific files

# Authentication and sensitive data
auth.json

# Caches and models
models_cache.json

# Session and history
history.jsonl
sessions/
archived_sessions/

# Local state and internal storage
.codex-global-state.json
internal_storage.json
.personality_migration
version.json

# Logs and temporary files
log/
tmp/

# Vendor and shell snapshots
vendor_imports/
shell_snapshots/

# Database files
sqlite/

# System files
.DS_Store
`;

const GEMINI_GITIGNORE = `# Generated by agentprofiles
# Keep: settings.json, GEMINI.md, AGENTS.md, skills/
# Ignore: browser profiles, installation IDs, state, temporary files

# Browser and installation data
antigravity/
antigravity-browser-profile/
installation_id
user_id

# Settings and state
settings.json.orig
state.json

# Temporary files
tmp/

# System files
.DS_Store
`;

const AUGMENT_GITIGNORE = `# Generated by agentprofiles
# Keep: settings.json, agents/, commands/
# Ignore: session tokens, caches, binaries, internal state, logs

# Session and authentication
.auggie.json
session.json
sessions/

# Caches and temporary files
checkpoint-documents/
task-storage/
tmp/

# Binaries and internal state
binaries/
electron-node/
auggie-path

# Logs and debugging
mcp-debug.log
mcp-server/
observability/

# System files
.DS_Store
`;

export function getAgentGitignore(agent: string) {
  switch (agent) {
    case 'claude':
      return CLAUDE_GITIGNORE;
    case 'opencode':
      return OPENCODE_GITIGNORE;
    case 'amp':
      return AMP_GITIGNORE;
    case 'codex':
      return CODEX_GITIGNORE;
    case 'gemini':
      return GEMINI_GITIGNORE;
    case 'augment':
      return AUGMENT_GITIGNORE;
    default:
      return '';
  }
}
