connector:
  id: env-test
  mode: batch

sources:
  data:
    kind: sql
    sql: "SELECT * FROM (VALUES ('A', 'hello')) AS t(id, val)"
    primaryKey: id

pipelines:
  entities:
    - source: data
      steps:
        - id: cp
          kind: checkpoint
          name: "${CHECKPOINT_NAME}"
