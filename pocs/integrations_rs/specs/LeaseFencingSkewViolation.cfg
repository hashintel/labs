\* Dishonest deployment: real clock disagreement exceeds the declared
\* clock_skew envelope (A4 violated) while the protocol still uses the
\* declared value for grace and chunk-window arithmetic. Expect: TLC finds
\* a ChunkExclusion counterexample, showing that the safety argument needs
\* the envelope and that the spec is falsifiable by exactly the fault it
\* exists to exclude.
CONSTANTS
  Workers = {w1, w2}
  NoOwner = NoOwner
  LeaseDurations = {6}
  GraphChunkDeadlines = {1}
  CursorCommitDeadlines = {1}
  SafetyMargins = {1}
  ClockSkew = 1
  ActualSkewBound = 4
  MaxTime = 10
  MaxVer = 5
  ChunkSlots = 2
INIT Init
NEXT Next
INVARIANTS TypeOK ChunkExclusion
CONSTRAINT StateBound
SYMMETRY Sym
CHECK_DEADLOCK FALSE
